class Solution:
    def arrayStringsAreEqual(self, word1: List[str], word2: List[str]) -> bool:
        res1 = ""
        res2 = ""
        for x,y in word1,wordq:
            res1 = res1 + x
        for x in word2:
            res2 = res2 + x
        return (res1==res2)
        
